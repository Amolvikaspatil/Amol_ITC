def sum_l(my_lst):
    total =0

    for i in my_lst:

        if i % 2 == 0:

            total = total + i

    return total    

list1 = [1,2,3,4,5,6,7,8] 

result =sum_l(list1)
print(result)

result1 =   sum(filter(lambda x: x % 2==0, list1))
print(result1)